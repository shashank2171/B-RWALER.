{
  "name": "projectx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd crawler && serve -s build\" \"cd backend && node server/index.js\"",
    "build": "npm install && cd backend && npm install && cd ../crawler && npm install && npm run build "

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
